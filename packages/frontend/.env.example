# WalletConnect v2 project ID. Required for the WalletConnect mobile/QR
# connection path. Create a free project ID at https://cloud.walletconnect.com
# and copy it into .env (local) or the Vercel env vars (production).
WALLETCONNECT_PROJECT_ID=